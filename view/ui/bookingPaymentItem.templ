package ui

import (
	"booking-api/data/response"
    "fmt"
)

templ BookingPaymentItem(payment response.BookingPaymentResponse) {
	<div class="flex flex-col gap-2">
        <div>
            <span>${ fmt.Sprintf("%.2f", payment.PaymentAmount) }</span>
        </div>

	</div>
}

